pipeline {
    agent any

    stages {
        stage('stage1') {
            steps {
                script {
                    // myplayground
                    echo 'Hello, welcome to Jenkins PAC'
                }
            }
        }
    }
}
